# CS Intelligence System — Python dependencies
# Install with: pip install -r requirements.txt
# Requires Python 3.11+

# --- AI / Agent framework ---
langgraph==0.2.74          # LangGraph StateGraph (agent brain)
langchain-core==0.3.51     # Shared LangChain primitives used by LangGraph
openai==1.77.0             # GPT-4o for message generation
anthropic==0.49.0          # Claude API for reasoning tasks

# --- Supabase (account state & interaction logs) ---
supabase==2.15.1

# --- Integrations (Sprint 2) ---
simple-salesforce==1.12.6  # Salesforce REST API client
requests==2.32.3           # HTTP client for Zendesk & Morpheus APIs

# --- Data / validation ---
pydantic==2.11.3           # Data validation (used in agent state models)
python-dotenv==1.1.0       # Load .env files

# --- Dashboard ---
streamlit>=1.32.0          # Streamlit UI (st.container border=True requires 1.32+)

# --- Testing ---
pytest==8.3.5
pytest-asyncio==0.26.0     # Async test support for future LangGraph async nodes
